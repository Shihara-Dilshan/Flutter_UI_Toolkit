import 'package:flutter/material.dart';

class FLButton extends StatelessWidget {
  const FLButton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("custom button"),
    );
  }
}
